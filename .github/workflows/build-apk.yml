name: Build TRX Launcher APK

on:
  workflow_dispatch:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

permissions:
  contents: read

concurrency:
  group: apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build installable APK
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Verify Google Navigation key
        env:
          MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}
        run: |
          if [ -z "$MAPS_API_KEY" ]; then
            echo "::error::Repository secret MAPS_API_KEY is missing. Add the restricted Google Navigation SDK key before building."
            exit 1
          fi

      - name: Build release APK
        env:
          MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}
        run: gradle --no-daemon --stacktrace :app:assembleRelease

      - name: Verify APK internal version
        run: |
          AAPT_BIN=$(find "$ANDROID_HOME/build-tools" -type f -name aapt | sort -V | tail -1)
          "$AAPT_BIN" dump badging app/build/outputs/apk/release/app-release.apk | grep "versionCode='55' versionName='2.1.0'"

      - name: Give APK a friendly name
        run: cp app/build/outputs/apk/release/app-release.apk TRX-Launcher-v2.1.0.apk

      - name: Upload downloadable APK
        uses: actions/upload-artifact@v4
        with:
          name: TRX-Launcher-v2.1.0
          path: TRX-Launcher-v2.1.0.apk
          if-no-files-found: error
          retention-days: 30
